<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_scan"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="ymc.p2ptransmit.main.ScanActivity">

    <ListView
        android:id="@+id/listview_user_scan"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/radarView"
        android:layout_alignLeft="@+id/radarView"
        android:layout_alignStart="@+id/radarView"
        android:layout_marginTop="21dp"
        android:layout_alignRight="@+id/radarView"
        android:layout_alignEnd="@+id/radarView" />

    <ymc.p2ptransmit.view.RadarView
        android:layout_width="280dp"
        android:layout_height="280dp"
        android:id="@+id/radarView"
        android:layout_marginTop="32dp"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <TextView
        android:text="@string/user_scanning"
        android:textSize="20sp"
        android:gravity="center_horizontal"
        android:typeface="serif"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/textView" />

</RelativeLayout>
